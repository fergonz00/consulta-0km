feat: tres origenes de consulta (stock / reparto / sin disponibilidad)

El vendedor ahora elige primero de que bolsa sale el auto y el wizard le muestra
solo eso, en vez de un unico dropdown que mezclaba todo.

- stock: solo chasis fisicos libres + a recibir.
- reparto: solo las unidades que VW nos ofrece en un color que NO tenemos en stock
  (de 56 unidades en reparto, hoy 24 aportan algo nuevo). Ademas avisa que colores
  del modelo si estan en el salon.
- sin disponibilidad (NUEVO): el catalogo entero, incluidos los 20 modelos que hoy
  no tienen ninguna unidad y eran inconsultables. El vendedor elige modelo + color
  de la paleta real del modelo, y el precio es opcional: la consulta puede ser solo
  para saber si el auto se consigue.

Para el admin, bloque "que hay de lo que piden": veredicto arriba (esta / esta en
reparto / no esta ese color / no hay nada del modelo), colores del modelo en stock
y en reparto, y cuando fue la ultima vez que tuvimos esa combinacion modelo+color.
Respuesta con "se consigue" / "no se consigue" / "en gestion con el zonal"; este
ultimo corta los recordatorios sin cerrar la consulta.

Edge stock-disponible: ahora devuelve catalogoModelos (48 con precio publicado) y
paleta (nombreCorto -> colores reales, 18 meses de Oversoft + todo reparto_vw).

Gotcha resuelto: Oversoft y reparto_vw escriben distinto el mismo color
(Gris Volcan/Volcan, Blanco puro/Puro, Gris Indy/Indy metalizado). Sin normalizar,
una Amarok que esta en el salon figuraba como color faltante. normColor() en la Edge
y _normColor() en el front son gemelos: hay que cambiarlos juntos.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
